variant ->
    plain version
    matching
    optional
    children
        -> repeat

regex creator ->
    ([0-9]+V|[0-9]+|-[0-9]+)
        is equal
        or less to
    <=
    =<
    is less
        than -> optional
        or equal to
